package pub.avalonframework.sqlhelper.core.beans;

/**
 * @author baichao
 */
public enum JoinType {
    /**
     * inner join
     */
    INNER,
    /**
     * left join
     */
    LEFT,
    /**
     * right join
     */
    RIGHT
}